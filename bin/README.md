Manage my prison – Scripts
==========================

These scripts can be run locally with `./bin/NAME.ts`
whereas on the server, where the Typescript runtime is unavailable, they should be run using `npm run NAME`.
